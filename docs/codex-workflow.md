# Codex Workflow Notes

## Visual verification

- Use the Chrome plugin for visual checks and browser QA when working on Sunrise UI behavior.
- Keep code-level checks in the normal local toolchain; use Chrome when rendered layout, interaction, authentication, navigation, or browser-specific behavior needs verification.
